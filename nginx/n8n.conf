# NGINX Configuration for n8n
# Save as: /etc/nginx/conf.d/n8n.conf

server {
    listen 80;
    server_name YOUR_DOMAIN_HERE;  # Change this!

    # Maximum upload size
    client_max_body_size 20M;

    # n8n proxy
    location / {
        proxy_pass http://localhost:5678;
        proxy_http_version 1.1;
        
        # WebSocket support (required for n8n editor)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        
        # Standard proxy headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        proxy_cache_bypass $http_upgrade;
    }
}

# Optional: If you want to host upload site on same server
# Uncomment and configure:

# server {
#     listen 80;
#     server_name upload.yourdomain.com;
#     
#     root /var/www/html/secondsite;
#     index index.html;
#     
#     location / {
#         try_files $uri $uri/ =404;
#     }
#     
#     location ~ \.php$ {
#         proxy_pass http://localhost:8080;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }
#     
#     location /uploads {
#         alias /var/www/html/secondsite/uploads;
#     }
# }
